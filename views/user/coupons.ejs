
<div class=" fixed  top-0 left-0 w-full z-50">
    <%- include('../partials/header') %>
</div>


<div class="flex mt-[72px] relative">

    <div class="fixed ">
        <%- include('../partials/userSlidebar') %>
    </div>

    <div class=" w-full ml-56 ">

        <h1 class="font-bold text-2xl ml-32 mt-4">My Coupons</h1>

<div class="mt-4">
    <% coupon.forEach(each=>{%>
    <div class="max-w-xl bg-white p-4 rounded-lg shadow-lg flex items-center mt-2 ml-48 space-x-4">
        <div>
            <p class="text-green-600 font-semibold text-lg">Coupon Code:<%= each.couponCode %> <span class=" ml-2 text-green-600"> <%=each.discountValue%><%= each.couponType=="price" ?  ' Rs' : '%' %> off </span></p>
            <p class="text-gray-700 mt-1">
                order above ₹<%=each.minimumAmount%> 
                <span class="text-gray-500">(Valid till:<%=each.endDate.toLocaleString()%> )</span>
            </p>
        </div>
        <div class="text-right">
            <p class="text-gray-500 text-sm">Valid till:<%=each.endDate.toLocaleString()%> </p>
            <!-- <a href="#" class="text-blue-600 font-medium text-sm hover:underline">View T&C</a> -->
        </div>
    </div>
    <%})%>
    
</div>